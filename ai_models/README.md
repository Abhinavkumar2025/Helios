# AI Models

Placeholder packages for the computer-vision and incident-classification APIs.

Each model directory can later expose a common prediction interface without coupling the dashboard to a specific model implementation.